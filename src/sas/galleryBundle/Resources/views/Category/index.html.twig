{% extends 'galleryBundle::layout_admin.html.twig' %}

{% block content %}
    <h1>Catégories</h1>
    {# <a href="{{ path('category_new') }}">+ Ajouter</a> #}
    <a href="{{ path('admin') }}"><< Retour à l'accueil</a>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Description</th>
                {# <th>Actions</th> #}
            </tr>
        </thead>
        <tbody>
            {% for entity in entities %}
                <tr>
                    <td>{# <a href="{{ path('category_show', { 'id': entity.id }) }}"> #}{{ entity.id }}{# </a> #}</td>
                    <td>{{ entity.name }}</td>
                    <td>{{ entity.description }}</td>
                    {# <td>
                    <ul>
                        <li>
                            <a href="{{ path('category_show', { 'id': entity.id }) }}">Voir</a>
                        </li>
                        <li>
                            <a href="{{ path('category_edit', { 'id': entity.id }) }}">Editer</a>
                        </li>
                    </ul>
                    </td> #}
                </tr>
            {% endfor %}
        </tbody>
    </table>

    {% endblock %}
