{% extends 'galleryBundle::layout_admin.html.twig' %}

{% block content %}
    <h1>Category Editer</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('category') }}">
            << Retour à la liste
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
